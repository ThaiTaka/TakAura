# README Media Assets

Thư mục này chứa media hiển thị trên `ai_training/README.md`.

## Files đang dùng

- `results.png` — biểu đồ training tổng quan
- `labels.jpg` — phân bố labels
- `confusion_matrix_normalized.png` — ma trận nhầm lẫn chuẩn hóa
- `box_pr_curve.png` — PR curve

## Gợi ý nâng cấp visual

Bạn có thể thêm các file sau để README “ngầu” hơn nữa:

- `demo.gif` — clip inference realtime từ app
- `banner.png` — banner dự án kích thước ngang (tỉ lệ 16:6)

Sau đó sửa các link ảnh trong `ai_training/README.md` để trỏ sang file mới.
